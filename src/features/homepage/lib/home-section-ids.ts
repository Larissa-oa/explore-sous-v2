/** In-page anchor below the hero (category tiles); keep in sync with skip-link `href` in `HomeHero`. */
export const HOME_MAIN_CONTENT_ID = "home-main-content";

/** Delivery product rail anchor; explore “See all” scroll target. */
export const HOME_DELIVERY_RAIL_ID = "home-delivery-rail";

/** Pickup product rail anchor — below delivery. */
export const HOME_PICKUP_RAIL_ID = "home-pickup-rail";
